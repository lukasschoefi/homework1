# Text Analyzer Project

[![Ruff](https://img.shields.io/endpoint?url=https://raw.githubusercontent.com/astral-sh/ruff/main/assets/badge/v2.json)](https://github.com/astral-sh/ruff)

## Scope
This is a small, stand-alone Python project that provides simple string manipulation and analysis functions. It was created as part of a university/course assignment.

## Features
* `clean_whitespace`: Removes extra spaces from strings.
* `count_words`: Calculates the total word count of a string.
* `is_palindrome`: Checks if a given text reads the same forwards and backwards.

## AI Usage Declaration
**Important:** AI (Google Gemini) was used in the creation of this project.
* **Workflow:** The AI was used to quickly draft the boilerplate code, structure the GitHub Actions YAML file, and generate the unit tests to meet a tight deadline. 
* **Contribution:** The core logic of the three functions, the test file, and this README structure were generated by the AI and manually reviewed by me before committing.

## Setup
1. Clone the repository.
2. Install dependencies: `pip install -r requirements.txt`
3. Run tests: `pytest`
4. Run linter: `ruff check .`
